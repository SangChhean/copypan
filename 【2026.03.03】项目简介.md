# Copypan 项目简介

## 一、项目概述

Copypan 是一个面向圣经与讲道纲目场景的**智能检索与写作辅助平台**。支持基于 Elasticsearch 的传统检索、基于大模型的 **AI 纲目生成**（RAG）、AI 智能问答、纲目翻译（中英互译）、简繁互转、格式刷与 DOCX/PDF 导出等能力，为讲章撰写与资料整理提供一站式支持。

项目采用前后端分离：前端为单页应用（SPA），后端为 REST API 服务，通过 JWT/Cookie 鉴权，支持多用户与管理员后台。

---

## 二、技术栈

### 2.1 前端

| 技术 | 用途 |
|------|------|
| **Vue 3** | 框架（Composition API + `<script setup>`） |
| **Vite** | 构建与开发服务器 |
| **Vue Router** | 路由（Hash 模式） |
| **Pinia** | 状态管理（`src/store/index.js`） |
| **Ant Design Vue 4** | UI 组件库（表格、表单、消息、通知等） |
| **Axios** | HTTP 请求 |
| **Less** | 样式预处理 |

前端代码位于 `front_mic/frontend/`，入口为 `src/main.js`，路由在 `src/router/index.js`，首页为 `Index.vue`（内嵌 `Header.vue` + `Search.vue`），工具箱与用户、管理、状态页等组件在 `src/components/`。

### 2.2 后端

| 技术 | 用途 |
|------|------|
| **Python 3.10+** | 运行环境 |
| **FastAPI** | Web 框架与 API 路由 |
| **Uvicorn** | ASGI 服务器 |
| **Pydantic** | 请求/响应模型校验 |
| **Elasticsearch 8.x** | 全文检索与数据存储（`requirements.txt` 要求 8.0.x；7.x 可通过 `scripts/reindex_local.py` 迁移后使用） |
| **Anthropic Claude** | AI 纲目生成、AI 问答与 RAG；毛胚纲目（润色/初信/青少年/真理/三分钟分享） |
| **Google Gemini** | 纲目翻译（中翻英/英翻中）；毛胚纲目（润色、三分钟分享） |
| **OpenAI / Deep Seek / Perplexity / xAI Grok** | 毛胚纲目「三分钟分享」多模型（填写对应 API Key 后启用） |
| **Redis** | AI 搜索缓存与监控统计 |
| **PyJWT** | 登录态与鉴权 |
| **python-docx** | DOCX 读写与纲目格式刷 |
| **docx2pdf / LibreOffice** | DOCX 转 PDF（Windows 可用 Word COM，Linux 用 LibreOffice） |
| **zhconv / OpenCC** | 简繁转换 |
| **cn2an** | 数字格式（如壹、贰）处理 |

后端代码位于 `back_mic/backend/`，主入口为 `main.py`，**AI 纲目生成与检索**在 `ai_search/`（`ai_router.py`、`ai_service.py`），传统检索在 `search/`，用户与鉴权在 `user/`，格式刷在 `format_chinese_outline.py`、`format_english_outline.py`。

### 2.3 数据与基础设施

- **Elasticsearch**：索引圣经、讲道、词典、节期纲目等数据，供传统搜索与 AI 纲目/问答检索；**当前推荐 ES 8.x**（与 `requirements.txt` 一致），7.x 可经 `scripts/reindex_local.py` 迁移。双路混合检索（BM25 + 向量 + RRF）与向量索引见设计文档 `ai_search/DESIGN_SPEC_V2.md`，待实现。
- **Redis**：缓存 AI 问答/纲目结果、记录调用统计与错误（可选，未配置时仅禁用缓存）。
- **文件系统**：模板 DOCX、上传数据等存放在后端目录；临时 DOCX/PDF 使用系统临时目录，用后清理。

---

## 三、主要功能与实现简述

### 3.1 首页与 AI 纲目面板（核心）

登录后首页为「搜索」页（`Search.vue`，由 `Index.vue` 引入），页面内包含**传统检索**与 **AI 纲目面板**两大块，其中 **AI 纲目面板**是核心能力。

#### AI 纲目面板入口与表单

- **入口**：页面中可展开「AI 纲目」面板（`aiPanelVisible`），与普通搜索并列。
- **必填**：**纲目主题**（题目，如「神圣的生命」）、**纲目性质**（一般性 / 高真理浓度 / 高生命浓度 / 重实行应用）；可选填写负担说明、特殊需要、面对对象等，用于检索权重与生成上下文。
- **深度**：可选「一般」（约 50 条上下文）或「深度」（约 200 条上下文），影响召回数量与生成质量。

#### AI 纲目生成流程（当前实现）

1. **检索**  
   - **一步模式**：调用 `/api/ai_search`，后端在 Elasticsearch 中按题目与纲目性质做多索引 BM25 检索（`_multi_index_search`），得到相关职事段落。  
   - **两步模式（方案 A）**：先调用 `/api/ai_search/search` 仅做检索，返回引用来源与 `search_id`；再调用 `/api/ai_search/generate` 根据 `search_id` 从 Redis 取上下文并生成纲目，便于前端先展示来源再展示答案。
2. **生成**  
   后端将「题目 + 纲目性质等元数据 + 检索到的段落」交给 **Claude Sonnet**，按既定 prompt 生成**中文纲目**（壹贰叁层级、读经、经文引用等），结果返回前端展示。
3. **多语言与格式**  
   - **中文纲目**：直接展示，支持复制、刷格式并下载 DOCX/PDF（`/api/ai_search/format_outline_only`，direction 为 en2zh 表示中文纲目）。  
   - **英文纲目**：用户可勾选「同时生成英文纲目」，中文纲目生成完成后自动调用 `/api/ai_search/translate_outline`（Gemini）翻译，得到英文纲目与英文标题，同样支持复制、刷格式并下载。  
   - **繁体纲目**：可勾选「同时生成繁体纲目」，调用 `/api/ai_search/outline_to_traditional` 将简体转为台湾繁体，支持复制、刷格式并下载。
4. **下载顺序**  
   前端对每种语言固定先请求 DOCX 再请求 PDF，避免多线程下 Windows COM 初始化顺序问题。

#### 检索与生成的技术细节（当前与规划）

- **当前**：Elasticsearch 8.x（或迁移后的 7.x），BM25 多索引检索；按纲目性质（`special_needs`）选择不同索引权重（如高真理浓度时 cwwl 1994–1997 加权）；map 类索引（map_note、map_7feasts、map_dictionary、map_pano）使用 nested `msg` 与 inner_hits 提取命中小节。
- **规划（PanAI 向量升级）**：见 `ai_search/DESIGN_SPEC_V2.md`，引入双路混合检索（路 1 BM25，路 2 题目展开 → Embedding → kNN），两路 RRF 合并后取 Top 35～40 段送入 Claude；通过 `USE_VECTOR_SEARCH` 环境变量可回退为纯 BM25（实现待完成）。

#### 首页其他：传统检索与一步 AI 问答

- **传统检索**：输入关键词、选择分类与索引，调用后端 `search/` 模块在 ES 中做全文检索，结果带高亮。
- **一步 AI 问答**：同一页面也可仅做「问题 → 检索 → Claude 生成答案」（不强调纲目结构），即经典 RAG 问答，与 AI 纲目共用 `/api/ai_search` 及缓存、监控。

### 3.2 工具箱

- **定制搜索（新约职事词典）**：`/cws` → `Cwws.vue`，调用 `/api/cws`，后端从 ES 指定索引检索并返回结果。
- **经文汇集**：`/bibco` → `BibleCo.vue`，调用 `/api/getvers`，按输入经文范围汇集内容。
- **信息检索**：`/info-retrieval` → `InfoRetrieval.vue`，调用 `/api/ai_search/info_retrieval`，按关键词与排除词检索，导出为 DOCX（单文件 40MB 或 100MB 上限可选，超出则多文件）。
- **纲目翻译**：`/outline-translate` → `OutlineTranslate.vue`，支持中→英、英→中；翻译调用 `/api/ai_search/outline_translate`（Gemini），格式化与下载调用 `/api/ai_search/format_outline_only`，支持多选 DOCX/PDF。
- **简繁互转**：`/zh-convert` → `ZhConvert.vue`，支持简体↔台湾繁体；转换调用 `/api/ai_search/outline_to_traditional` 或 `traditional_to_simplified`，格式化与下载走 `format_outline_only` 或 `convert_and_format`。
- **毛胚纲目**：`/rough-outline` → `RoughOutline.vue`，按类型（润色/初信/青少年/真理/三分钟分享）调用不同 AI 生成毛胚纲目，支持刷格式并下载。  
  - **润色**：4 次生成（2×Gemini 3.0 Pro + 2×Claude Sonnet 4.5），每次单独调用 `/api/ai_search/rough_outline`（`ai_index` 0～3）。  
  - **初信/青少年/真理**：各 1 次 Claude Sonnet 4.5。  
  - **三分钟分享**：6 次生成（Claude、Gemini、Deep Seek、Perplexity、OpenAI ChatGPT、xAI Grok），需在 `.env` 中填写对应 API Key 后启用。
- **节期纲目**：`/feast-outline` → `FeastOutline.vue`，支持五类（纲目的原文、带经文的纲目、晨兴信息选读的纲目、听抄稿的纲目、复合的纲目）；听抄稿支持序言/添言原文，由后端生成序言/添言纲目并参与刷格式与下载。

### 3.3 纲目格式与 PDF

- **格式刷**：中文纲目使用 `format_chinese_outline_docx`（标题、读经、壹贰参层级、标点与数字规范化等）；英文纲目使用 `format_english_outline_docx`。繁体输出时启用 `traditional_quotes=True`，统一为『』「」。
- **DOCX→PDF**：`ai_service._convert_docx_to_pdf` 优先用 LibreOffice（Linux，PDF/A-2b 以嵌入字体），失败则回退到 `docx2pdf`（Windows 需当前线程 `CoInitialize`，已在转换前调用）。

### 3.4 用户与权限

- **登录 / 注册 / 改密 / 忘记密码**：`/login`、`/signup`、`/changepass`、`/forgot`，后端 `/api/token`、`/api/signup`、`/api/changePass` 等，鉴权依赖 JWT，可选 Cookie（session）与 Authorization 头。
- **鉴权**：大部分 API 通过 `test_token` 或 `OAuth2PasswordBearer` 校验 JWT；管理员接口（如 `/api/datalist`、`/api/process`、`/api/upload`）额外校验角色为 `t0`。

### 3.5 管理后台

- **入口**：`/manage` → `ManaIndex.vue`，需管理员登录。
- **能力**：数据列表、上传 JSON、将数据导入 Elasticsearch（`/api/process`）、用户/邀请码/角色管理（`/api/usr_opts`、`/api/iv_opts` 等）；AI 搜索监控与运维（`AIStats.vue`、`AIStatsRetrieval.vue`、`AIStatsErrors.vue`，对应 `/api/ai_search/stats`、错误列表等）；导入进度通过 WebSocket `/api/ws/progress` 推送到前端。

### 3.6 AI 搜索监控与运维

- **健康与统计**：`/api/ai_search/health`、`/api/ai_search/stats`、`/api/ai_search/stats/errors`，基于 Redis 的调用次数与错误记录；可选重置统计、清理缓存（`/api/ai_search/stats/reset`、`/api/ai_search/cache/clear`）。
- **并发控制**：AI 路由中耗时同步调用（检索、Claude、Gemini、格式刷、PDF 转换等）均通过 `asyncio.to_thread` 执行，避免阻塞事件循环；Claude/Gemini 使用信号量限制并发（可选环境变量 `CLAUDE_CONCURRENT_LIMIT`、`GEMINI_CONCURRENT_LIMIT`）。

---

## 四、目录结构概览

```
copypan/
├── 【2026.03.01】项目简介.md   # 本文件
├── 开发环境清单.md             # 同事克隆、修改、上传所需环境与包清单
├── front_mic/frontend/         # 前端 SPA（Vue3 + Vite + Ant Design Vue）
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/index.js
│       ├── store/index.js      # Pinia
│       └── components/         # 首页（Index.vue → Header + Search.vue）、用户、工具箱、管理、状态页等
│           ├── Search.vue      # 含传统检索与 AI 纲目面板
│           ├── toolbox/        # Cwws, BibleCo, InfoRetrieval, OutlineTranslate, ZhConvert, RoughOutline, FeastOutline
│           └── management/     # ManaIndex, DataList, UploadList, UserOpts, IvOpts, AIStats, AIStatsRetrieval, AIStatsErrors
└── back_mic/backend/           # 后端 FastAPI
    ├── main.py                 # 应用入口、CORS、通用 API 与路由挂载
    ├── ai_search/             # AI 纲目生成与检索、纲目翻译、简繁、格式刷与 PDF、毛胚/节期纲目
    │   ├── ai_router.py
    │   ├── ai_service.py
    │   ├── monitoring.py
    │   ├── rough_outline_prompts.py
    │   ├── feast_outline_prompts.py
    │   └── DESIGN_SPEC_V2.md   # 双路混合检索设计（待实现）
    ├── search/                 # 传统 ES 检索
    ├── user/                   # 登录、注册、改密、JWT
    ├── database/               # 上传与数据列表
    ├── format_chinese_outline.py
    ├── format_english_outline.py
    ├── es_config.py
    ├── es_init.py
    ├── scripts/                # reindex_local（7→8）、vectorize_*、add_embedding_mapping 等
    └── requirements.txt
```

---

## 五、环境与运行

- **前端**：在 `front_mic/frontend/` 下执行 `npm install`、`npm run dev`，默认开发地址如 `http://localhost:5173`；需通过环境变量（如 `VITE_API_BASE`）配置后端 API 基础地址。
- **后端**：在 `back_mic/backend/` 下安装依赖（见 `requirements.txt`），配置 `.env`（复制 `.env.example`，填写 Elasticsearch、Redis、`CLAUDE_API_KEY`、`GEMINI_API_KEY`；毛胚纲目「三分钟分享」可选 `DEEPSEEK_API_KEY`、`PERPLEXITY_API_KEY`、`OPENAI_API_KEY`、`XAI_API_KEY`），运行 `uvicorn main:app` 等。
- **Elasticsearch、Redis** 需提前部署；推荐 **Elasticsearch 8.x**。Linux 下 PDF 导出依赖 LibreOffice，Windows 下可选 Microsoft Word（docx2pdf）。

以上为当前项目技术栈与功能的简要说明，其中 **AI 纲目面板**（首页 `Search.vue` 内）是面向讲道纲目生成的核心入口。双路混合检索（BM25 + 向量 + RRF）与题目展开流程见 `ai_search/DESIGN_SPEC_V2.md`，待实现后可通过 `USE_VECTOR_SEARCH` 启用。

*最后更新：2026 年 3 月 3 日 16：30*
