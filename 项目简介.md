# Copypan 项目简介

## 一、项目概述

Copypan 是一个面向圣经与讲道纲目场景的**智能检索与写作辅助平台**。支持基于 Elasticsearch 的传统检索、基于大模型的 AI 智能问答（RAG）、纲目翻译（中英互译）、简繁互转、格式刷与 DOCX/PDF 导出等能力，为讲章撰写与资料整理提供一站式支持。

项目采用前后端分离：前端为单页应用（SPA），后端为 REST API 服务，通过 JWT/Cookie 鉴权，支持多用户与管理员后台。

---

## 二、技术栈

### 2.1 前端

| 技术 | 用途 |
|------|------|
| **Vue 3** | 框架（Composition API + `<script setup>`） |
| **Vite** | 构建与开发服务器 |
| **Vue Router** | 路由（Hash 模式） |
| **Pinia** | 状态管理 |
| **Ant Design Vue 4** | UI 组件库（表格、表单、消息、通知等） |
| **Axios** | HTTP 请求 |
| **Less** | 样式预处理 |

Nginx

前端代码位于 `front_mic/frontend/`，入口为 `src/main.js`，路由在 `src/router/index.js`，主要页面与工具箱组件在 `src/components/`。

### 2.2 后端

| 技术 | 用途 |
|------|------|
| **Python 3** | 运行环境 |
| **FastAPI** | Web 框架与 API 路由 |
| **Uvicorn** | ASGI 服务器 |
| **Pydantic** | 请求/响应模型校验 |
| **Elasticsearch 7.x** | 全文检索与数据存储 |
| **Anthropic Claude** | AI 问答与 RAG 生成 |
| **Google Gemini** | 纲目翻译（中翻英 / 英翻中） |
| **Redis** | AI 搜索缓存与监控统计 |
| **PyJWT** | 登录态与鉴权 |
| **python-docx** | DOCX 读写与纲目格式刷 |
| **docx2pdf / LibreOffice** | DOCX 转 PDF（Windows 用 Word COM，Linux 用 LibreOffice） |
| **zhconv / OpenCC** | 简繁转换 |
| **cn2an** | 数字格式（如壹、贰）处理 |

后端代码位于 `back_mic/backend/`，主入口为 `main.py`，AI 搜索与纲目相关逻辑在 `ai_search/`（`ai_router.py`、`ai_service.py`），传统检索在 `search/`，用户与鉴权在 `user/`，格式刷在 `format_chinese_outline.py`、`format_english_outline.py`。

### 2.3 数据与基础设施

- **Elasticsearch**：索引圣经、讲道、词典等数据，供传统搜索与 AI RAG 检索。
- **Redis**：缓存 AI 问答结果、记录调用统计与错误（可选，未配置时仅禁用缓存）。
- **文件系统**：模板 DOCX、上传数据等存放在后端目录；临时 DOCX/PDF 使用系统临时目录，用后清理。

---

## 三、主要功能与实现简述

### 3.1 首页与 AI 智能搜索

- **入口**：登录后首页为「搜索」页（`Search.vue`），支持输入问题、选择深度（一般/深度）、可选填写纲目主题、负担说明等。
- **一步搜索**：调用 `/api/ai_search`，后端先按问题在 Elasticsearch 中检索相关段落，再将「问题 + 检索结果」交给 Claude 生成答案，返回答案与引用来源；支持 Redis 缓存相同问题。
- **两步搜索（方案 A）**：先调用 `/api/ai_search/search` 仅做检索并返回 `search_id`，再调用 `/api/ai_search/generate` 根据 `search_id` 生成答案，便于在前端分步展示。
- **AI 纲目生成**：在同一页面可基于检索与生成结果，选择「中文 / 英文 / 繁体」纲目，进行「刷格式并下载」：后端按选定语言调用对应格式刷（`format_chinese_outline_docx` / `format_english_outline_docx`），生成 DOCX，再按需转为 PDF 返回；前端固定先请求 DOCX 再请求 PDF，避免多线程下 Windows COM 初始化顺序问题。

### 3.2 工具箱

- **定制搜索（新约职事词典）**：`/cws` → `Cwws.vue`，调用 `/api/cws`，后端从 ES 指定索引检索并返回结果。
- **经文汇集**：`/bibco` → `BibleCo.vue`，调用 `/api/getvers`，按输入经文范围汇集内容。
- **信息检索**：`/info-retrieval` → `InfoRetrieval.vue`，调用 `/api/ai_search/info_retrieval`，按关键词与排除词检索，导出为 DOCX（单文件约 40MB 上限，超出则多文件）。
- **纲目翻译**：`/outline-translate` → `OutlineTranslate.vue`，支持中→英、英→中；翻译调用 `/api/ai_search/outline_translate`（Gemini），格式化与下载调用 `/api/ai_search/format_outline_only`，支持多选 DOCX/PDF，顺序固定为先 DOCX 再 PDF。
- **简繁互转**：`/zh-convert` → `ZhConvert.vue`，支持简体↔台湾繁体；转换调用 `/api/ai_search/outline_to_traditional` 或 `traditional_to_simplified`，格式化与下载同样走 `format_outline_only`，繁体纲目使用台湾用语与『』「」等标点（`format_chinese_outline.py` 的 `traditional_quotes`）。

### 3.3 纲目格式与 PDF

- **格式刷**：中文纲目使用 `format_chinese_outline_docx`（标题、读经、壹贰参层级、标点与数字规范化等）；英文纲目使用 `format_english_outline_docx`。繁体输出时启用 `traditional_quotes=True`，统一为『』「」。
- **DOCX→PDF**：`ai_service._convert_docx_to_pdf` 优先用 LibreOffice（Linux，PDF/A-2b 以嵌入字体），失败则回退到 `docx2pdf`（Windows 需当前线程 `CoInitialize`，已在转换前调用）。

### 3.4 用户与权限

- **登录 / 注册 / 改密**：`/login`、`/signup`、`/changepass`，后端 `/api/token`、`/api/signup`、`/api/changePass`，鉴权依赖 JWT，可选 Cookie（session）与 Authorization 头。
- **鉴权**：大部分 API 通过 `test_token` 或 `OAuth2PasswordBearer` 校验 JWT；管理员接口（如 `/api/datalist`、`/api/process`、`/api/upload`）额外校验角色为 `t0`。

### 3.5 管理后台

- **入口**：`/manage` → `ManaIndex.vue`，需管理员登录。
- **能力**：数据列表、上传 JSON、将数据导入 Elasticsearch（`/api/process`）、用户/邀请码/角色管理（`/api/usr_opts`、`/api/iv_opts` 等）；导入进度通过 WebSocket `/api/ws/progress` 推送到前端。

### 3.6 AI 搜索监控与运维

- **健康与统计**：`/api/ai_search/health`、`/api/ai_search/stats`、`/api/ai_search/stats/errors`，基于 Redis 的调用次数与错误记录；可选重置统计、清理缓存（`/api/ai_search/stats/reset`、`/api/ai_search/cache/clear`）。
- **并发控制**：AI 路由中耗时同步调用（检索、Claude、Gemini、格式刷、PDF 转换等）均通过 `asyncio.to_thread` 执行，避免阻塞事件循环；Claude/Gemini 使用信号量限制并发。

---

## 四、目录结构概览

```
copypan/
├── 项目简介.md           # 本文件
├── front_mic/frontend/   # 前端 SPA（Vue3 + Vite + Ant Design Vue）
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/
│       ├── store/        # Pinia
│       └── components/   # 首页、用户、工具箱、管理、状态页等
└── back_mic/backend/     # 后端 FastAPI
    ├── main.py           # 应用入口、CORS、通用 API 与路由挂载
    ├── ai_search/        # AI 搜索、纲目翻译、简繁、格式刷与 PDF
    ├── search/           # 传统 ES 检索
    ├── user/             # 登录、注册、改密、JWT
    ├── database/         # 上传与数据列表
    ├── format_chinese_outline.py
    ├── format_english_outline.py
    ├── es_config.py
    └── requirements.txt
```

---

## 五、环境与运行

- **前端**：在 `front_mic/frontend/` 下执行 `npm install`、`npm run dev`，默认开发地址如 `http://localhost:5173`；需通过环境变量（如 `VITE_API_BASE`）配置后端 API 基础地址。
- **后端**：在 `back_mic/backend/` 下安装依赖（见 `requirements.txt`），配置 `.env`（如 Elasticsearch、Redis、`CLAUDE_API_KEY`、`GEMINI_API_KEY` 等），运行 `uvicorn main:app` 等。
- **Elasticsearch、Redis** 需提前部署；Linux 下 PDF 导出依赖 LibreOffice，Windows 下可选 Microsoft Word（docx2pdf）。

以上为当前项目技术栈与功能的简要说明，便于新人快速了解整体架构与实现方式。
