# Pansearch（`copypan`）项目简介（2026-04）

`copypan` 当前是一个“双应用并存”的仓库：

- **Pansearch 主站**：原有检索/阅读/AI 工具体系（`back_mic` + `front_mic`）。
- **QA 问答站**：新拆分的职事信息问答系统（`back_qa` + `front_qa`），以 `/api/qa` 为主入口。
- **共享能力层**：`back_shared` 提供检索、重排、Neo4j 客户端、版本清单等复用模块。

---

## 当前技术栈（按模块）

| 模块 | 技术 |
|---|---|
| QA 后端 | Python + FastAPI（`back_qa/main.py`，默认端口 `8001`） |
| QA 前端 | Vue 3 + Vite + Ant Design Vue（`front_qa`） |
| 主站后端 | Python + FastAPI（`back_mic/backend/main.py`） |
| 主站前端 | Vue 3 + Vite（`front_mic/frontend`） |
| 检索 | Elasticsearch 8.x（多索引检索） |
| 缓存/监控 | Redis（缓存、限流、统计） |
| 图谱 | Neo4j（概念、希腊原文、关键经文等） |
| 模型 | Anthropic Claude 为主（QA Step1/3/4） |

---

## QA 系统（最新重点）

### 1) QA API 路由与鉴权

- 根路由前缀：`/api/qa`
- 核心接口：
  - `GET /api/qa/liveness`
  - `GET /api/qa/readiness`
  - `POST /api/qa/query`
- 用户接口：
  - `POST /api/qa/auth/register`
  - `POST /api/qa/auth/login`
  - `GET /api/qa/auth/me`
- 管理接口（`X-Admin-Token`）：
  - `GET /api/qa/stats`
  - `POST /api/qa/cache/clear`
  - 邀请码/用户管理相关 `/api/qa/auth/...`

### 2) QA 四步流水线（`back_qa/qa/qa_service.py`）

- **Step1 概念抽取**：从词表抽取 `surface` / `deep` 概念。
- **Step2 检索融合**：BM25 + Dense + RRF + Rerank，支持 deep 概念扩展检索。
- **Step3 相关性判断**：判定检索结果是否足够回答问题。
- **Step4 答案生成**：严格基于检索段落输出，附引用书目。
- 附加能力：定向查询预判、防火墙并发校验、Redis 缓存、请求监控统计。

### 3) QA 前端

- 目录：`front_qa`
- 主要依赖：`vue`、`vue-router`、`pinia`、`axios`、`ant-design-vue`。
- 本地开发默认配合 QA 后端 `127.0.0.1:8001` 使用。

---

## 主站（Pansearch）现状

主站功能仍保留在 `back_mic/backend` 与 `front_mic/frontend`，包括传统搜索阅读、KG-RAG、AI 工具箱、圆桌等能力。当前仓库处于“主站持续可用 + QA 独立扩展”的状态。

---

## 仓库关键目录（当前）

```text
copypan/
├── back_mic/                 # 主站后端（原 Pansearch）
├── front_mic/                # 主站前端
├── back_qa/                  # QA 后端（/api/qa）
├── front_qa/                 # QA 前端
├── back_shared/              # 共享检索/图谱/版本模块
├── start_all.bat             # 主站常用启动脚本
├── start_qa.bat              # QA 一键启动（后端8001+前端5174）
├── stop_qa.bat / status_qa.bat
├── ARCHITECTURE.md
├── DEPLOYMENT.md
├── back_qa/DEPLOY.md         # QA 部署说明
└── 项目简介.md               # 本文件
```

---

## 运行与部署要点

- QA 后端默认读取 `back_mic/backend/.env`（共享环境变量），并使用 `QA_*` 变量控制端口、模型、缓存等。
- QA 后端启动：`python -m uvicorn back_qa.main:app --host 0.0.0.0 --port 8001`
- QA 一键本地启动：执行 `start_qa.bat`（会启动 `back_qa` 与 `front_qa`）。
- 依赖基础设施：Elasticsearch、Redis、Neo4j。
- 服务器部署参考：`back_qa/DEPLOY.md`。

---

## 文档维护约定

- 本文件用于“快速了解当前仓库真实结构与职责边界”。
- 若目录、路由、启动方式或模块边界有变动，请优先同步本文件。
- 细节与实现以对应代码目录和部署文档为准（尤其是 `back_qa/DEPLOY.md` 与各 `main.py` / `router.py`）。
