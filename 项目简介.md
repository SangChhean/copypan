# Pansearch（copypan）项目简介

**Pansearch** 是一套面向圣经与倪柝声、李常受等职事文献的 **AI 辅助检索与阅读网站**（仓库名 `copypan`）。用户可登录后对多类索引做全文检索、阅读章节，并结合 **KG-RAG 知识增强检索**、**AI 工具箱（纲目翻译、节期纲目、毛坯纲目、信息检索导出等）**、**圆桌辩论** 等能力；管理员可维护用户、邀请码、数据导入与索引，并查看 AI 使用统计与缓存管理。

---

## 技术栈（概要）

| 层级 | 技术 |
|------|------|
| 后端 | Python **FastAPI**（Python 3.10+），JWT 鉴权（支持 Header `Authorization: Bearer` 与 Cookie `session`） |
| 前端 | **Vue 3** + **Vite** + **Ant Design Vue**，Hash 路由 |
| 检索 | **Elasticsearch 8.x**（IK 中文分词），多索引、分页与高亮 |
| 缓存 / 限流 | **Redis**（AI 搜索缓存与监控；未连接时部分能力降级，后端仍可启动） |
| 图谱 | **Neo4j**（KG-RAG 概念关系；连接失败时服务端可降级处理） |
| AI 供应商 | 依模块配置 **Anthropic（Claude）**、**Google（Gemini）**、**OpenAI** 等（见 `requirements.txt` 与各模块环境变量） |

更细的目录与历史接口说明见根目录 **`ARCHITECTURE.md`**（注意：其中部分路径未反映当前统一的 **`/api` 前缀**，以 `back_mic/backend/main.py` 与各 `*router.py` 为准）。

---

## API 路径约定

- **传统业务接口**（登录、搜索、阅读、管理端等）挂载在 **`/api`** 下，例如：`/api/token`、`/api/search`、`/api/reading`、`/api/ws/progress`（WebSocket 导入进度）。
- **AI 搜索与工具箱** 位于 **`/api/ai_search/...`**（健康检查 `GET /api/ai_search/health` 可匿名；其余多数需登录；统计、清缓存等需管理员）。
- **圆桌** 位于 **`/api/roundtable/...`**。
- **KG-RAG** 位于 **`/api/kg_rag/...`**：`POST /api/kg_rag/query` 与缓存翻译相关接口对**已登录用户**开放；健康检查、Prompt 预览、图谱浏览等部分接口仅**管理员**可用。

---

## 核心功能模块

### 1. 传统检索与阅读

- **主搜索**：按关键词与分类（`args`）在多个 ES 索引（如 bib、life、cwwn、cwwl、feasts 等）中联合检索，返回列表与高亮片段。
- **阅读**：按 `refid` 从 `pan_reading` 等索引取章节内容。
- **工具**：新约职事词典（cws）、圣经书卷/经文检索、节期/纲目相关工具页等（以实际前端菜单为准）。

### 2. KG-RAG（`kg_rag`）

- 结合 **Neo4j 概念关系** 与 **Elasticsearch 检索** 的流水线：概念抽取 → 骨架扩展 → 检索融合 → 生成答案（可与前端「主搜索」流式体验配合，典型调用为 **`POST /api/kg_rag/query`**）。
- 支持缓存中的英文/繁体纲目字段更新（**`POST /api/kg_rag/cache_translation`**，已登录）。
- 管理端 **工具箱中的 KG-RAG 测试台** 可调试全流程、Prompt 预览、图谱探索等（管理员权限接口）。

### 3. AI 工具箱（`ai_search`）

- **健康与运维**：`GET /api/ai_search/health`；管理员可见 **用量统计**（含 RAG、工具箱、圆桌费用汇总）、**错误记录**、**缓存清理** 等。
- **纲目类**：中文→英文纲目、简繁转换与 **DOCX/PDF** 格式化下载；工具箱 **纲目翻译**、**仅格式刷**；**毛坯纲目**（多类型、多模型轮询配置）；**节期纲目**（原文/带经文/晨兴/听抄稿/复合等多条流水线，长任务需注意网关超时）。
- **信息检索**：多关键词与排除词条件从 ES 导出 **DOCX**（见 `info_retrieval` 接口说明）。

长任务（尤其节期纲目、听抄稿类）若前有 **Nginx 等反向代理**，需调大读超时，说明见 **`back_mic/docs/gateway-timeout-feast-outline.md`**。

### 4. 圆桌辩论（`roundtable`）

- 多模型按场景对话，支持 **`scene_one`～`scene_four`** 等（如十二支派、神学辩论、重大讨论、顶级模型思考等），含会话记录与置顶等能力（详见 `roundtable/roundtable_router.py`）。

### 5. 用户与数据管理

- **用户**：注册（邀请码）、登录、改密码；角色区分普通用户与管理员。
- **管理端**：索引列表与清空、JSON 上传与批量导入 ES、WebSocket 导入进度、邀请码与用户管理；以及 **AI 使用统计** 子页（依赖 `ai_search` 监控接口）。

---

## 仓库主要路径

```
copypan/
├── README.md                 # 一行说明
├── ARCHITECTURE.md           # 架构与接口（部分路径需对照 /api 前缀）
├── 项目简介.md               # 本文件（面向阅读者的总览）
├── 开发环境清单.md           # 本机依赖（Python/Node/ES/Redis 等）
├── back_mic/backend/         # FastAPI 后端（main.py 入口）
├── front_mic/frontend/       # Vue 前端（npm run dev / build）
├── start_all.bat / stop_all.bat  # 本地启停脚本（若你环境中有配置）
└── back_mic/docs/            # 运维备忘（如网关超时）
```

---

## 运行与配置（原则性说明）

- 后端依赖 **`back_mic/backend/requirements.txt`**，需可用的 **Elasticsearch 8.x**；启用 KG-RAG 完整能力还需 **Neo4j** 及对应数据/索引配置。
- **Redis** 推荐开启，以使用 AI 缓存与监控统计；详见 **`开发环境清单.md`**。
- AI 相关功能需在环境中配置各厂商 **API Key**（如 Claude、Gemini、OpenAI），详见各模块或部署文档（如 **`DEPLOYMENT.md`**、**`DEPLOY_AI_SEARCH.md`**）。
- 前端通过 **`VITE_API_BASE`** 指向后端 API 根地址（开发时常见为 `http://127.0.0.1:端口`，路径仍使用上述 **`/api/...`**）。

---

## 文档维护说明

本简介用于快速了解 **项目定位与模块划分**；细节以各子目录代码及 **`开发环境清单.md`**、部署文档为准。若功能有较大变更，请同步更新本文件；**`ARCHITECTURE.md`** 若与当前路由不一致，宜以 **`main.py` 与各路由模块** 为准修订。

---

*用于替代此前按日期命名的项目简介快照；内容按当前仓库结构整理。*
