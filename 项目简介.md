# Pansearch（copypan）项目简介

**Pansearch** 是一套面向圣经与倪柝声、李常受等职事文献的 **AI 辅助检索与阅读网站**（仓库名 `copypan`）。用户可登录后对多类索引做全文检索、阅读章节，并使用 **AI 纲目生成、圆桌辩论、KG-RAG 知识增强检索** 等能力；管理员可维护用户、邀请码、数据导入与索引。

---

## 技术栈（概要）

| 层级 | 技术 |
|------|------|
| 后端 | Python **FastAPI**，JWT 鉴权（支持 Header Bearer 与 Cookie） |
| 前端 | **Vue 3** + **Vite** + **Ant Design Vue**，Hash 路由 |
| 检索 | **Elasticsearch**（IK 中文分词），多索引、分页与高亮 |
| 其他 | **Neo4j**（KG-RAG 概念图谱）、可选 **Jina** 等精排；部分 AI 能力依赖 **Claude / OpenAI** 等外部 API |

更细的目录与接口表见根目录 **`ARCHITECTURE.md`**。

---

## 核心功能模块

### 1. 传统检索与阅读

- **主搜索**：按关键词与分类（args）在多个 ES 索引（如 bib、life、cwwn、cwwl、feasts 等）中联合检索，返回列表与高亮片段。
- **阅读**：按 `refid` 从 `pan_reading` 等索引取章节内容。
- **工具**：新约职事词典（cws）、圣经书卷/经文检索、节期/纲目相关工具页等（以实际前端菜单为准）。

### 2. AI 搜索与纲目（`ai_search`）

- 基于检索结果与 LLM 的 **AI 问答 / 深度检索**，以及 **听抄稿纲目、粗纲目、节期纲目** 等生成流程（具体路由见 `ai_search/ai_router.py`）。
- 长任务可能耗时数分钟；若前有 **Nginx 等反向代理**，需调大读超时，说明见 **`back_mic/docs/gateway-timeout-feast-outline.md`**。

### 3. 圆桌辩论（`roundtable`）

- 多模型轮流发言、总结等对话能力（独立路由模块，供前端圆桌页使用）。

### 4. KG-RAG（`kg_rag`）

- 结合 **Neo4j 概念关系** 与 **ES 向量/BM25 检索** 的流水线：概念抽取 → 骨架扩展 → 检索融合 → 可选生成答案。
- 提供 **健康检查、全流程查询、仅检索、Prompt 预览、图谱浏览** 等接口；管理端 **工具箱中的 KG-RAG 测试台** 用于调试与对比多模型（Claude / GPT 等）。

### 5. 用户与数据管理

- **用户**：注册（邀请码）、登录、改密码；角色区分普通用户与管理员。
- **管理端**：索引列表与清空、JSON 上传与批量导入 ES、WebSocket 导入进度、邀请码与用户管理。

---

## 仓库主要路径

```
copypan/
├── README.md                 # 一行说明
├── ARCHITECTURE.md           # 架构与接口较完整说明
├── 项目简介.md               # 本文件（面向阅读者的总览）
├── back_mic/backend/         # FastAPI 后端（main.py 入口）
├── front_mic/frontend/       # Vue 前端（npm run dev / build）
├── start_all.bat / stop_all.bat  # 本地启停脚本（若你环境中有配置）
└── back_mic/docs/            # 运维备忘（如网关超时）
```

---

## 运行与配置（原则性说明）

- 后端依赖 **`requirements.txt`**，需可用的 **Elasticsearch**（及 KG-RAG 所需的 **Neo4j**、相关索引与向量字段）。
- AI 相关功能需在环境中配置对应厂商的 **API Key**（如 Claude、OpenAI），详见各模块说明或环境变量约定。
- 前端通过 **`VITE_API_BASE`** 指向后端 API 根地址（开发时常见为代理或直连 `http://127.0.0.1:端口`）。

---

## 文档维护说明

本简介用于快速了解 **项目定位与模块划分**；细节以 **`ARCHITECTURE.md`** 与各子目录代码为准。若功能有较大变更，请同步更新本文件与架构文档中的相关小节。

---

*文档生成说明：用于替代此前按日期命名的项目简介快照；内容按当前仓库结构整理，日期以仓库迭代为准。*
