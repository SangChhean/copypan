# Pansearch（`copypan`）项目简介（2026-06）

`copypan` 当前是一个「双应用并存 + 共享能力层 + 学生练习区」的仓库：

- **Pansearch 主站**：原有检索 / 阅读 / AI 工具体系（`back_mic` + `front_mic`），默认端口 `8000` / `5173`。
- **QA 问答站**：独立拆分的职事信息问答系统（`back_qa` + `front_qa`），以 `/api/qa` 为主入口，默认端口 `8001` / `5174`。
- **CN 中国站**：独立进程的中国区应用（`back_cn` + `front_cn`），业务 API 保持 `/api/qa/*`，鉴权 `/api/cn/auth/*`，默认端口 `8014` / `5176`。
- **共享能力层**：`back_shared` 提供检索、重排、Embedding、Neo4j 客户端、版本清单等复用模块。
- **学生练习区**：`testC`、`test_B`、`testD` 等目录承载作业与实验功能，经 `main.py` 挂载路由接入主站工具箱。

---

## 当前技术栈（按模块）

| 模块 | 技术 |
|---|---|
| 主站后端 | Python + FastAPI（`back_mic/backend/main.py`，默认 `8000`） |
| 主站前端 | Vue 3 + Vite + Ant Design Vue（`front_mic/frontend`，Hash 路由） |
| 主站鉴权 | JWT + HttpOnly Session Cookie（`/api/token`） |
| QA 后端 | Python + FastAPI（`back_qa/main.py`，默认 `8001`） |
| QA 前端 | Vue 3 + Vite + Ant Design Vue（`front_qa`） |
| QA 鉴权 | SQLite（`back_qa/qa/qa_users.db`）+ JWT（`QA_JWT_SECRET`） |
| CN 后端 | Python + FastAPI（`back_cn/main.py`，默认 `8014`） |
| CN 鉴权 | SQLite（`back_cn/cn_users.db`）+ JWT（`CN_JWT_SECRET`），五组日配额（Asia/Shanghai） |
| CN 前端 | Vue 3 + Vite + Ant Design Vue 按需引入（`front_cn`，默认 `5176`） |
| 检索 | Elasticsearch 8.x（多索引 BM25 + Dense + RRF） |
| 精排 | Jina Reranker（`back_shared/reranker_service.py`） |
| 缓存 / 限流 / 监控 | Redis |
| 图谱 | Neo4j（概念、希腊原文、关键经文等） |
| 主站 LLM | Claude（KG-RAG、纲目制作）、Gemini（增强式翻译、部分翻译）、DeepSeek（文章润色） |
| QA 主模型 | Anthropic Claude（Step1 默认 Opus、Step3/定向预判 Haiku、Step4 Sonnet，见 `back_shared/version_manifest.py`） |
| 翻译 | Gemini（英文）、OpenCC + 术语表（繁体） |
| TTS | Gemini Flash TTS / MiniMax（QA 后端）；前端默认 AWS Polly Lambda |
| ASR | OpenAI Whisper（`POST /api/qa/asr`） |

---

## 主站（Pansearch）

### 1) 核心能力

- **传统搜索 / 阅读**：`POST /api/search`、`/api/cws`（词典）、`/api/reading`（阅读模式）。
- **经文汇集**：`POST /api/getvers`（`tools/biblecollection.py`），支持中英文经文查询与 DOCX 格式下载（`/api/getvers/format_download`、`format_download_zh`）。
- **管理后台**：`#/manage`（数据导入、用户 / 邀请码管理、ES 索引操作等，需 `t0` 角色）。
- **静态托管**：生产构建后 `front_mic/frontend/dist` 由后端根路径挂载；`index.html` 禁缓存，带 hash 的 `assets/` 长期缓存。

### 2) 工具箱（`#/tools`）

工具箱按功能分组，路由定义于 `front_mic/frontend/src/router/index.js`。

**定制搜索**

| 路由 | 功能 |
|---|---|
| `#/cws` | 新约职事的词典搜索 |

**工具**

| 路由 | 功能 | 主要 API |
|---|---|---|
| `#/bibco` | 经文汇集 | `/api/getvers` |
| `#/info-retrieval` | 信息检索（关键词导出 DOCX） | `/api/ai_search/info_retrieval` |
| `#/outline-translate` | 纲目翻译（中↔英） | `/api/ai_search/outline_translate` |
| `#/enhanced-translate-official` | **增强式翻译（主站正式版）** | `/api/ai_search/enhanced_translate` |
| `#/zh-convert` | 简繁互转 | `/api/ai_search/outline_to_traditional` 等 |
| `#/article-polish` | 文章润色 | `/api/ai_search/polish`（DeepSeek） |
| `#/roundtable` | AI 圆桌 | `/api/roundtable/*` |
| `#/feast-outline-maker` | 节期数据制作 | `/api/feast/*` |

**纲目制作**

| 路由 | 功能 |
|---|---|
| `#/rough-outline` | 毛胚纲目 |
| `#/feast-outline` | 节期纲目 |
| `#/ministerialize-outline` | 纲目职事化 |
| `#/bird-view-outline` | 词典-鸟瞰纲目 |

**图谱测试**

| 路由 | 功能 |
|---|---|
| `#/kg-rag-test` | KG-RAG 测试工作台（需登录） |

### 3) 增强式翻译（两套并存）

| 版本 | 代码位置 | 前端路由 | API 前缀 |
|---|---|---|---|
| **主站正式版** | `back_mic/backend/ai_search/enhanced_translate_*` | `#/enhanced-translate-official` | `/api/ai_search/enhanced_translate` |
| **学生作业版（testD）** | `testD/backend/` | `#/enhanced-translate` | `/api/kg_rag/enhanced_translate` |

主站正式版流程：逐行解析纲目 → Additional Pool 短路 → BM25 + Dense + RRF + Rerank 检索职事语料 → Gemini 批量中翻英；支持人工修正写回 Pool（`/update_translation`）。详见 `testD/README.md`（学生版设计说明与接线方式）。

### 4) KG-RAG 纲目制作（`/api/kg_rag`）

`kg_rag/kg_rag_service.py` 编排 Step 1→5 全流程：

- **Step 1**：概念抽取（Claude）
- **Step 2**：骨架构建 + 三路检索融合（BM25 / Dense / 骨架路由）
- **Step 3–4**：Neo4j 图谱扩展、防火墙校验
- **Step 5**：纲目生成（支持一般性 / 真理启示 / 生命经历 / 应用实行等性质）

附加能力：

- **鸟瞰纲目**：`/api/kg_rag/bird_view/skeleton`、`outline`、`source`
- **纲目职事化**：`/api/kg_rag/ministerialize`、`ministerialize_docx`
- **负担说明生成**：`/api/kg_rag/generate_burden`
- **图谱探索**（管理员）：`/api/kg_rag/graph/explore`、`path`、`stats`

版本与缓存：`PROMPT_VERSION = v1.1`、`FIREWALL_RULES_VERSION = v1.0`、`MODEL_PROFILE = sonnet-4-6`（`back_shared/version_manifest.py`）。

### 5) 学生练习区挂载

`main.py` 将以下练习路由接入主站工具箱（分组见 `ToolBox.vue`）：

| 目录 | 说明 |
|---|---|
| `testC/` | Chom Pei 组：纲目翻译练习、简繁互转练习、AI 纲目制作练习等 |
| `test_B/` | Piseth 组：纲目翻译测试、简繁互转测试、PanAI2 纲目制作等 |
| `testD/` | Sotchea 组：增强式翻译学生作业版 |

本地开发：`cd front_mic/frontend && npm run dev`（`5173`），后端 `cd back_mic/backend && python main.py`（`8000`）；可用 `start_all.bat` 一键拉起 Docker 基础设施 + 主站。

---

## QA 系统

### 1) 入口与生命周期

- 应用入口：`back_qa/main.py`
- 启动时加载 `back_qa/bible_data/*.json` 到内存；连接 Neo4j 并每 5 分钟刷新数据基线。
- 环境变量默认从 `back_mic/backend/.env` 加载（与主站共享）。
- 单次请求超时 120 秒（`TimeoutMiddleware`）。

### 2) API 路由与鉴权

根前缀：`/api/qa`。除注明外，用户接口需 `Authorization: Bearer <JWT>`。

**健康检查（无需登录）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/qa/liveness` | 进程存活 |
| GET | `/api/qa/readiness` | ES / Redis / Neo4j 及数据基线、prompt 版本 |

**问答核心**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/qa/query` | 非流式四步流水线（调试 / 脚本用） |
| POST | `/api/qa/stream` | **前端主路径**：SSE 流式问答（计日配额） |
| POST | `/api/qa/bible/query` | 经文专用 SSE（计日配额；前端亦可通过 `/stream` 走 bible 分支） |
| POST | `/api/qa/translate` | 答案简 / 繁 / 英翻译 |
| POST | `/api/qa/tts` | Gemini TTS（WAV） |
| POST | `/api/qa/tts/minimax` | MiniMax TTS（MP3） |
| POST | `/api/qa/asr` | 语音转文字（Whisper，≤10MB） |
| POST | `/api/qa/feedback` | 答案赞 / 踩（±1） |

**用户与配额**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/qa/auth/register` | 邀请码注册（公开） |
| POST | `/api/qa/auth/login` | 登录，返回 JWT（公开） |
| GET | `/api/qa/auth/me` | 当前用户 |
| GET | `/api/qa/auth/usage` | 当日问答用量（不递增） |

**管理（`X-Admin-Token` = `QA_ADMIN_TOKEN`）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/qa/stats` | 请求监控统计 |
| POST | `/api/qa/stats/clear` | 清空监控 |
| POST | `/api/qa/cache/clear` | 清理 `qa:cache:*` |
| GET | `/api/qa/feedback/stats` | 反馈汇总 |
| POST | `/api/qa/auth/invite` | 创建邀请码 |
| GET | `/api/qa/auth/invites` | 邀请码列表 |
| GET | `/api/qa/auth/users` | 用户列表 |
| DELETE | `/api/qa/auth/users/{username}` | 删除用户 |

限流：`QA_RATE_LIMIT_PER_MINUTE`（默认 15 次/分钟/IP）。日配额：`QA_DAILY_LIMIT`（默认 30），对 `/stream` 与 `/bible/query` 生效。

### 3) 问答流水线（`back_qa/qa/qa_service.py`）

**意图预判（Step 0）**：Haiku 判断 `general` / `targeted`（定向书目+信息）/ `bible`（经文节/范围/整章）。

**通用职事问答**

- **Step1 概念抽取**：从词表抽取 `surface` / `deep` 概念（默认 `claude-opus-4-6`，可用 `QA_STEP1_MODEL` 覆盖）。
- **Step2 检索融合**：BM25 + Dense + RRF + Rerank；支持 deep 概念扩展检索。
- **Step3 相关性判断**：判定段落是否足以回答问题。
- **Step4 答案生成**：严格基于检索段落生成，附引用书目（模型见 `MODEL_PROFILE`，当前 `sonnet-4-6`）。

**分支与附加能力**

- **定向查询**：命中书目+关键词时走快捷检索路径。
- **经文问答**：加载 `back_qa/bible_data`，展示经文后经独立 Step4 生成解释（`bible_service.py`）。
- **防火墙**：并发 LLM 校验（`firewall_rules.json`，版本见 `FIREWALL_RULES_VERSION`）。
- **缓存**：Redis，`QA_REDIS_PREFIX` + `QA_CACHE_TTL`；版本字段集中在 `back_shared/version_manifest.py`。
- **监控**：Redis 列表记录每次请求耗时、费用、是否命中缓存等。

### 4) QA 前端（`front_qa`）

| 路由 | 组件 | 说明 |
|---|---|---|
| `#/login` | `LoginPage.vue` | 邀请码注册 / 登录 |
| `#/` | `QAPage.vue` | 主对话（SSE `/api/qa/stream`） |
| `#/admin` | `AdminPage.vue` | 统计、反馈、邀请码与用户管理 |

主要交互：多轮对话、追问书名补全、经文展示（`BibleMessage.vue`）、简繁英切换、语音输入（ASR）、朗读（Polly / 可选 Gemini·MiniMax）、答案反馈、日配额展示。静态 PDF 置于 `front_qa/public/lsm/`。

本地开发：后端 `127.0.0.1:8001`，前端 Vite `127.0.0.1:5174`（`start_qa.bat` 一键启动）。

---

## CN 中国站

- 应用入口：`back_cn/main.py`；启动序列：ES/Neo4j → `KgRagService` → 防火墙 → `bible_data` → Redis → `cn_users.db`。
- 环境：先加载 `back_mic/backend/.env`，再由 `back_cn/.env` 覆盖（Redis 监控/限流等键前缀隔离，见 `back_cn/.env.example`）。
- 探活：`GET /api/cn/liveness`、`GET /api/cn/readiness`。
- 鉴权：`/api/cn/auth/*`（独立用户库，五组日配额 outline/translate/qa/burden/asr；`usage` 接口仅展示前三组）。
- 本地开发：后端 `python -m uvicorn back_cn.main:app --host 127.0.0.1 --port 8014`；前端 `cd front_cn && npm run dev -- --port 5176`；或 `start_cn.bat`。

### CN 工具路由（路径与主站一致，鉴权走 CN JWT）

| 功能 | 方法 | 路径 | 配额 |
|---|---|---|---|
| 经文汇集 | POST | `/api/getvers` | 无 |
| 经文汇集 DOCX（英） | POST | `/api/getvers/format_download` | 无 |
| 经文汇集 DOCX（中） | POST | `/api/getvers/format_download_zh` | 无 |
| 纲目翻译 | POST | `/api/ai_search/outline_translate` | translate（仅 zh2en/en2zh） |
| 纲目格式化下载 | POST | `/api/ai_search/format_outline_only` | 无 |
| 简转繁 | POST | `/api/ai_search/outline_to_traditional` | 无 |
| 繁转简 | POST | `/api/ai_search/traditional_to_simplified` | 无 |
| 易错字检查 | POST | `/api/ai_search/check_error_chars` | 无 |

前端入口：`#/bibco`、`#/outline-translate`、`#/zh-convert`（`front_cn`）；纲目制作、资料下载仍为 Phase 4/5 占位。

---

## 仓库关键目录

```text
copypan/
├── back_mic/                 # 主站后端
│   └── backend/
│       ├── main.py           # FastAPI 入口，挂载全部路由
│       ├── ai_search/        # 纲目翻译、简繁互转、信息检索、增强式翻译等
│       ├── kg_rag/           # KG-RAG 流水线、鸟瞰纲目、职事化
│       ├── roundtable/       # AI 圆桌
│       └── tools/            # 经文汇集等
├── front_mic/frontend/       # 主站前端（工具箱、搜索、管理）
├── back_qa/                  # QA 后端
│   ├── main.py
│   ├── bible_data/           # 圣经 JSON（按卷）
│   ├── qa/                   # 路由、流水线、鉴权、ASR 等
│   ├── scripts/              # E2E、限流验证等
│   └── DEPLOY.md             # QA 部署说明
├── front_qa/                 # QA 前端
│   ├── src/components/       # QAPage、LoginPage、AdminPage、BibleMessage…
│   └── public/lsm/           # 职事 PDF 静态资源
├── back_cn/                  # CN 后端
│   ├── main.py               # FastAPI 入口（8014）
│   ├── auth.py               # cn_users.db + 五组日配额
│   ├── routers/              # auth / qa / bible / tools 路由
│   └── .env.example          # CN 专用与 Redis 前缀覆盖变量
├── front_cn/                 # CN 前端（5176，代理 /api → 8014）
├── back_shared/              # 共享检索 / 图谱 / 版本模块
├── testC/                    # 学生练习（Chom Pei 组）
├── test_B/                   # 学生练习（Piseth 组）
├── testD/                    # 增强式翻译学生作业（Sotchea 组）
├── scripts/                  # 评测、TTS/ASR 试验脚本等
├── start_all.bat             # 主站 + 基础设施检查
├── start_qa.bat              # QA 一键启动（8001 + 5174）
├── stop_qa.bat / status_qa.bat
├── start_cn.bat              # CN 一键启动（8014 + 5176）
├── stop_cn.bat / status_cn.bat
└── 项目简介.md               # 本文件
```

---

## 运行与部署要点

**本地主站**

```bash
# 需先启动 ES、Redis、Neo4j（可用 start_all.bat 或 Docker）
cd back_mic/backend && python main.py          # 8000
cd front_mic/frontend && npm run dev           # 5173
```

**本地 QA**

```bash
python -m uvicorn back_qa.main:app --host 0.0.0.0 --port 8001
# 或执行 start_qa.bat
```

**本地 CN**

```bash
python -m uvicorn back_cn.main:app --host 127.0.0.1 --port 8014
cd front_cn && npm run dev -- --port 5176
# 或执行 start_cn.bat
```

**关键环境变量**（写在 `back_mic/backend/.env`）

| 变量 | 用途 |
|---|---|
| `CLAUDE_API_KEY` | KG-RAG、QA Step1/3/4 |
| `GEMINI_API_KEY` | 增强式翻译、TTS、英文翻译 |
| `DEEPSEEK_API_KEY` | 文章润色 |
| `OPENROUTER_API_KEY` | 向量检索（增强式翻译 Dense 路） |
| `ENHANCED_TRANSLATE_GEMINI_MODEL` | 增强式翻译 Gemini 模型（默认 `gemini-3.5-flash`） |
| `QA_JWT_SECRET` | QA JWT 签名（必填，否则 QA 后端无法启动） |
| `QA_ADMIN_TOKEN` | QA 管理接口 |
| `QA_DAILY_LIMIT` | 日问答上限（默认 30） |
| `QA_PORT` / `QA_REDIS_PREFIX` / `QA_CACHE_TTL` / `QA_RATE_LIMIT_PER_MINUTE` | QA 服务与缓存 |
| `QA_STEP1_MODEL` / `QA_STEP3_MODEL` / `QA_ES_INDEX` | QA 模型与索引覆盖 |
| `OPENAI_API_KEY` | ASR |
| `MINIMAX_API_KEY` / `MINIMAX_GROUP_ID` | MiniMax TTS（可选） |
| `ES_*` / `REDIS_*` / `NEO4J_*` | 基础设施（主站与 QA 共享） |

**CN 专用**（写在 `back_cn/.env`，覆盖共享模块 Redis 前缀）

| 变量 | 用途 |
|---|---|
| `CN_PORT` | CN 服务端口（默认 `8014`） |
| `CN_JWT_SECRET` | CN JWT 签名（必填） |
| `CN_ADMIN_TOKEN` | CN 管理接口 `X-Admin-Token` |
| `CN_DAILY_LIMIT_OUTLINE` / `TRANSLATE` / `QA` / `BURDEN` / `ASR` | 五组日配额默认值 |
| `AI_MONITORING_PREFIX` | 主站 monitoring 键前缀（CN 默认 `cn_monitoring:`） |
| `QA_RATELIMIT_PREFIX` / `QA_MONITOR_KEY` / `QA_REDIS_PREFIX` | QA 模块键前缀（CN 进程内覆盖为 `cn:*`） |

**生产部署**：QA 见 `back_qa/DEPLOY.md`（Nginx 反代 `/api/qa/` → `8001`，静态托管 `front_qa/dist`）；主站通常 Nginx 反代 `/api/` → `8000`，静态托管 `front_mic/frontend/dist`。

---

## 文档维护约定

- 本文件用于快速了解当前仓库真实结构与职责边界。
- 目录、路由、启动方式或模块边界有变动时，请优先同步本文件。
- 实现细节以对应代码及 `back_qa/DEPLOY.md`、`testD/README.md`、`testD/WIRING.md` 为准。
