# Copypan 项目简介

## 一、项目概述

Copypan 是一个面向圣经与讲道纲目场景的**智能检索与写作辅助平台**。支持基于 Elasticsearch 的传统检索、基于大模型的 **AI 纲目生成**（RAG）、**AI 圆桌会议**（多 AI 协作讨论）、AI 智能问答、纲目翻译（中英互译）、简繁互转、格式刷与 DOCX/PDF 导出等能力，为讲章撰写与资料整理提供一站式支持。

项目采用前后端分离：前端为单页应用（SPA），后端为 REST API 服务，通过 JWT/Cookie 鉴权，支持多用户与管理员后台。**AI 纲目生成**已支持双路混合检索（BM25 + 向量 kNN）+ RRF 融合 + Jina Reranker 精排，以及两种生成方式：**方式一**（仅主题自由生成）、**方式二**（主题 + 负担说明/摘要，按框架生成）。**AI 圆桌会议**支持四种场景：**十二支派**（历史神学研究）、**神学辩论**（多轮交锋）、**重大讨论**（作答→互相点评→最终评价·总结）、**顶级模型思考**（单轮深度思考，从 Claude Opus 4.6 / GPT-5.4 / Gemini 3.1 Pro 三选一）；参与者为六大 AI（Claude、GPT、Gemini、Grok、DeepSeek、Perplexity）或场景④的单一大模型。

---

## 二、技术栈

### 2.1 前端

| 技术 | 用途 |
|------|------|
| **Vue 3** | 框架（Composition API + `<script setup>`） |
| **Vite** | 构建与开发服务器 |
| **Vue Router** | 路由（Hash 模式） |
| **Pinia** | 状态管理（`src/store/index.js`） |
| **Ant Design Vue 4** | UI 组件库（表格、表单、消息、通知等） |
| **Axios** | HTTP 请求 |
| **Less** | 样式预处理 |

前端代码位于 `front_mic/frontend/`，入口为 `src/main.js`，路由在 `src/router/index.js`，首页为 `Index.vue`（内嵌 `Header.vue` + `Search.vue`），工具箱与用户、管理、状态页等组件在 `src/components/`。

### 2.2 后端

| 技术 | 用途 |
|------|------|
| **Python 3.10+** | 运行环境 |
| **FastAPI** | Web 框架与 API 路由 |
| **Uvicorn** | ASGI 服务器 |
| **Pydantic** | 请求/响应模型校验 |
| **Elasticsearch 8.x** | 全文检索与数据存储（`requirements.txt` 要求 8.0.x；7.x 可通过 `scripts/reindex_local.py` 迁移后使用） |
| **Anthropic Claude** | AI 纲目生成、AI 问答与 RAG；毛胚纲目（润色/初信/青少年/真理/三分钟分享）；AI 圆桌会议 |
| **Google Gemini** | 纲目翻译（中翻英/英翻中）；毛胚纲目（润色、三分钟分享）；AI 圆桌会议 |
| **OpenAI GPT** | 毛胚纲目「三分钟分享」；AI 圆桌会议 |
| **DeepSeek** | 毛胚纲目「三分钟分享」；AI 圆桌会议 |
| **Perplexity** | 毛胚纲目「三分钟分享」；AI 圆桌会议 |
| **xAI Grok** | 毛胚纲目「三分钟分享」；AI 圆桌会议 |
| **Redis** | AI 搜索缓存与监控统计 |
| **SQLite** | AI 圆桌会议历史记录存储（含四类场景：十二支派/神学辩论/重大讨论/顶级模型思考） |
| **PyJWT** | 登录态与鉴权 |
| **python-docx** | DOCX 读写与纲目格式刷 |
| **docx2pdf / LibreOffice** | DOCX 转 PDF（Windows 可用 Word COM，Linux 用 LibreOffice） |
| **zhconv / OpenCC** | 简繁转换 |
| **cn2an** | 数字格式（如壹、贰）处理 |

后端代码位于 `back_mic/backend/`，主入口为 `main.py`，**AI 纲目生成与检索**在 `ai_search/`（`ai_router.py`、`ai_service.py`），**AI 圆桌会议**在 `roundtable/`（`roundtable_router.py`、`roundtable_service.py`、`ai_clients.py`），传统检索在 `search/`，用户与鉴权在 `user/`，格式刷在 `format_chinese_outline.py`、`format_english_outline.py`。

### 2.3 数据与基础设施

- **Elasticsearch**：索引圣经、讲道、词典、节期纲目等数据，供传统搜索与 AI 纲目/问答检索；**推荐 ES 8.x**（与 `requirements.txt` 一致）。**双路混合检索**（BM25 + 向量 kNN + RRF + Jina Reranker）已实现，通过 `USE_VECTOR_SEARCH=true` 启用，详见 `ai_search/DESIGN_SPEC_V2.md` 与 `ai_search/纲目制作RAG系统设计方案.md`。
- **Redis**：缓存 AI 问答/纲目结果、记录调用统计与错误（可选，未配置时仅禁用缓存）。
- **SQLite**：存储 AI 圆桌会议历史记录（`roundtable.db`），含场景类型、主题、参与者、发言记录、结论与费用。
- **文件系统**：模板 DOCX、上传数据等存放在后端目录；临时 DOCX/PDF 使用系统临时目录，用后清理。

---

## 三、主要功能与实现简述

### 3.1 首页与 AI 纲目面板（核心）

登录后首页为「搜索」页（`Search.vue`，由 `Index.vue` 引入），页面内包含**传统检索**与 **AI 纲目面板**两大块，其中 **AI 纲目面板**是核心能力。

#### AI 纲目面板入口与表单

- **入口**：页面中可展开「AI 纲目」面板（`aiPanelVisible`），与普通搜索并列。
- **必填**：**纲目主题**（题目）、**纲目性质**（一般性 / 高真理浓度 / 高生命浓度 / 重实行应用）。
- **可选**：**负担说明/简单摘要（约 50 字）**——有实质内容（≥10 字）时走**方式二**（按摘要解析成多个检索方向再生成）；留空则走**方式一**（仅按主题双路检索后自由生成）。面对对象等用于生成上下文。
- **深度**：普通（约 50 条上下文）或深度（约 200 条），影响召回数量与生成质量。

#### AI 纲目生成流程（当前实现）

1. **检索与分支**  
   - **方式一**（无负担说明或实质字数 &lt; 10）：Claude 将主题展开为 4～5 个短句（启示/真理/经历/应用等），对「主题 + 子句」做 **BM25 + 多路 kNN** 双路检索，RRF 融合后经 Jina Reranker 精排、索引加权，取 Top 15～60 条（依深度）送 Claude 自由生成纲目。  
   - **方式二**（有负担说明且实质字数 ≥ 10）：Claude 先将「主题 + 摘要」解析为 2～5 个检索方向（每点含 title、search_query、sub_directions），每个方向独立做 BM25 + kNN → RRF → Reranker → 索引加权，再按大点组织内容送 Claude **按框架填充**纲目。  
   - **一步模式**：调用 `/api/ai_search`，后端完成检索 + 生成并返回。  
   - **两步模式**：先调用 `/api/ai_search/search` 仅检索，返回引用来源与 `search_id`；再调用 `/api/ai_search/generate` 根据 `search_id` 从 Redis 取上下文并生成纲目，便于先展示来源再展示答案。
2. **生成**  
   方式一/方式二**共用**同一套纲目生成 system prompt（`_build_generate_system_prompt`），约束格式、逐字引用、经节标注等。Claude Sonnet 生成**中文纲目**后返回前端。
3. **多语言与格式**  
   - **中文纲目**：直接展示，支持复制、刷格式并下载 DOCX/PDF。  
   - **英文纲目**：勾选「同时生成英文纲目」后调用 `/api/ai_search/translate_outline`（Gemini）翻译。  
   - **繁体纲目**：勾选后调用 `/api/ai_search/outline_to_traditional` 转为台湾繁体。  
   均支持刷格式与 DOCX/PDF 下载。
4. **下载顺序**  
   前端对每种语言先请求 DOCX 再 PDF，避免多线程下 Windows COM 初始化顺序问题。

#### 检索与生成的技术细节

- **已实现**：Elasticsearch 8.x（或迁移后的 7.x）；`USE_VECTOR_SEARCH=true` 时启用**双路混合**（路 1 BM25 `_multi_index_search`，路 2 子主题/摘要方向 Embedding → kNN）；RRF（k=60）融合后 Jina Reranker v3 精排，再按纲目性质索引加权；方式一/二均返回统一结构的引用来源与 `claude_payload`。  
- **降级**：`USE_VECTOR_SEARCH=false` 时回退为纯 BM25，行为与改造前一致。  
- **详细设计**：见 `ai_search/DESIGN_SPEC_V2.md`、`ai_search/纲目制作RAG系统设计方案.md`。

#### 首页其他：传统检索与一步 AI 问答

- **传统检索**：输入关键词、选择分类与索引，调用后端 `search/` 模块在 ES 中做全文检索，结果带高亮。
- **一步 AI 问答**：同一页面也可仅做「问题 → 检索 → Claude 生成答案」（不强调纲目结构），即经典 RAG 问答，与 AI 纲目共用 `/api/ai_search` 及缓存、监控。

### 3.2 工具箱

- **定制搜索（新约职事词典）**：`/cws` → `Cwws.vue`，调用 `/api/cws`，后端从 ES 指定索引检索并返回结果。
- **经文汇集**：`/bibco` → `BibleCo.vue`，调用 `/api/getvers`，按输入经文范围汇集内容。
- **信息检索**：`/info-retrieval` → `InfoRetrieval.vue`，调用 `/api/ai_search/info_retrieval`，按关键词与排除词检索，导出为 DOCX（单文件 40MB 或 100MB 上限可选，超出则多文件）。
- **纲目翻译**：`/outline-translate` → `OutlineTranslate.vue`，支持中→英、英→中；翻译调用 `/api/ai_search/outline_translate`（Gemini），格式化与下载调用 `/api/ai_search/format_outline_only`，支持多选 DOCX/PDF。
- **简繁互转**：`/zh-convert` → `ZhConvert.vue`，支持简体↔台湾繁体；转换调用 `/api/ai_search/outline_to_traditional` 或 `traditional_to_simplified`，格式化与下载走 `format_outline_only` 或 `convert_and_format`。
- **毛胚纲目**：`/rough-outline` → `RoughOutline.vue`，按类型（润色/初信/青少年/真理/三分钟分享）调用不同 AI 生成毛胚纲目，支持刷格式并下载。  
  - **润色**：4 次生成（2×Gemini 3.0 Pro + 2×Claude Sonnet 4.5），每次单独调用 `/api/ai_search/rough_outline`（`ai_index` 0～3）。  
  - **初信/青少年/真理**：各 1 次 Claude Sonnet 4.5。  
  - **三分钟分享**：6 次生成（Claude、Gemini、DeepSeek、Perplexity、OpenAI ChatGPT、xAI Grok），需在 `.env` 中填写对应 API Key 后启用。
- **节期纲目**：`/feast-outline` → `FeastOutline.vue`，支持五类（纲目的原文、带经文的纲目、晨兴信息选读的纲目、听抄稿的纲目、复合的纲目）；听抄稿支持序言/添言原文，由后端生成序言/添言纲目并参与刷格式与下载。
- **AI 圆桌会议**：`/roundtable` → `RoundTable.vue`，详见 3.3 节。

### 3.3 AI 圆桌会议（新增）

AI 圆桌会议是一个多 AI 协作讨论功能，支持六大主流 AI 模型（Claude、GPT、Gemini、Grok、DeepSeek、Perplexity）围绕指定主题进行讨论。

#### 支持场景

| 场景 | 名称 | 说明 |
|------|------|------|
| `scene_one` | **十二支派** | 多 AI 并行进行历史神学研究，最后由 Claude 综合汇总出结论 |
| `scene_two` | **神学辩论** | AI 根据用户分配的立场进行 3 轮交锋辩论，最后由 Claude 给出中立结论 |
| `scene_three` | **重大讨论** | 第一轮各 AI 作答，第二轮互相点评（至少 2～3 人），第三轮对题目做最终评价，Claude 总结 |
| `scene_four` | **顶级模型思考** | 从 Claude Opus 4.6（thinking）、GPT-5.4（pro）、Gemini 3.1 Pro 中选一个，单轮深度思考，无总结 |

#### 功能特点

- **参与者选择**：场景①～③可选择 2～6 个 AI；场景④仅选 1 个（三选一：Claude Opus 4.6 / GPT-5.4 / Gemini 3.1 Pro）
- **立场分配**（神学辩论）：为每个 AI 分配立场（如「支持派」「反对派」等）
- **实时流式输出**：通过 SSE（Server-Sent Events）实时推送各 AI 发言
- **打字机效果**：前端按固定顺序逐字显示发言（场景④为单轮单模型）
- **历史记录**：所有会议记录存入 SQLite，支持查看详情、置顶、删除
- **费用统计**：每次会议记录各 AI 的 Token 消耗与费用

#### API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/roundtable/start` | POST | 创建圆桌会议 Session |
| `/api/roundtable/stream/{session_id}` | GET | SSE 流式获取发言内容 |
| `/api/roundtable/history` | GET | 获取历史记录列表 |
| `/api/roundtable/history/{record_id}` | GET | 获取单条记录详情 |
| `/api/roundtable/history/{record_id}/pin` | POST | 切换置顶状态 |
| `/api/roundtable/history/{record_id}` | DELETE | 删除记录 |
| `/api/roundtable/export/{record_id}` | GET | 导出为 DOCX/PDF |

#### 技术实现

- **后端**：`roundtable/` 目录下含 `roundtable_router.py`（API 路由）、`roundtable_service.py`（业务逻辑与 AI 调度）、`ai_clients.py`（统一 AI 调用封装）、`roundtable_prompts.py`（Prompt 模板）、`roundtable_db.py`（SQLite 存储）
- **前端**：`RoundTable.vue`（主会议页面）、`RoundTableDetail.vue`（历史记录详情页）
- **AI 调用**：使用 `asyncio.as_completed` 实现并发调用，各 AI 独立计费

### 3.4 纲目格式与 PDF

- **格式刷**：中文纲目使用 `format_chinese_outline_docx`（标题、读经、壹贰参层级、标点与数字规范化等）；英文纲目使用 `format_english_outline_docx`。繁体输出时启用 `traditional_quotes=True`，统一为『』「」。
- **DOCX→PDF**：`ai_service._convert_docx_to_pdf` 优先用 LibreOffice（Linux，PDF/A-2b 以嵌入字体），失败则回退到 `docx2pdf`（Windows 需当前线程 `CoInitialize`，已在转换前调用）。

### 3.5 用户与权限

- **登录 / 注册 / 改密 / 忘记密码**：`/login`、`/signup`、`/changepass`、`/forgot`，后端 `/api/token`、`/api/signup`、`/api/changePass` 等，鉴权依赖 JWT，可选 Cookie（session）与 Authorization 头。
- **鉴权**：大部分 API 通过 `test_token` 或 `OAuth2PasswordBearer` 校验 JWT；管理员接口（如 `/api/datalist`、`/api/process`、`/api/upload`）额外校验角色为 `t0`。

### 3.6 管理后台

- **入口**：`/manage` → `ManaIndex.vue`，需管理员登录。
- **能力**：数据列表、上传 JSON、将数据导入 Elasticsearch（`/api/process`）、用户/邀请码/角色管理（`/api/usr_opts`、`/api/iv_opts` 等）；AI 使用统计与监控；导入进度通过 WebSocket `/api/ws/progress` 推送到前端。

### 3.7 AI 使用统计（管理后台）

管理后台「AI 使用统计」菜单下分为六个子页面，提供细分的费用与调用统计：

| 子页面 | 组件 | 功能 |
|--------|------|------|
| **AI 纲目制作** | `AIStatsRAG.vue` | RAG 纲目生成统计（调用次数、费用、缓存命中率、响应时间）；纲目性质/使用模式/深度模式分布；每日统计；检索统计日志；错误记录；AI 检索权重配置展示 |
| **节期纲目** | `AIStatsFeast.vue` | 节期纲目 Claude 调用统计 |
| **毛胚纲目** | `AIStatsRoughOutline.vue` | 六 AI（Claude/Gemini/DeepSeek/GPT/Perplexity/Grok）分别的调用次数与费用 |
| **纲目翻译** | `AIStatsTranslation.vue` | 中→英、英→中翻译调用统计 |
| **AI 圆桌** | `AIStatsRoundtable.vue` | 圆桌会议统计（场景分布：十二支派/神学辩论/重大讨论/顶级模型思考；每日统计） |
| **费用总览** | `AIStatsSummary.vue` | 五大模块费用汇总与占比；每日费用趋势 |

#### 相关 API

| 端点 | 说明 |
|------|------|
| `/api/ai_search/stats` | 原有统计接口（含 retrieval_log、index_weights） |
| `/api/ai_search/stats/detail` | 新增详细统计接口（供六个子页面使用，返回 rag/toolbox/roundtable/summary 四大块） |
| `/api/ai_search/stats/errors` | 错误记录列表 |
| `/api/ai_search/stats/reset` | 重置统计（双重确认） |
| `/api/ai_search/cache/clear` | 清理 AI 搜索缓存 |

### 3.8 AI 搜索监控与运维

- **健康与统计**：`/api/ai_search/health`、`/api/ai_search/stats`、`/api/ai_search/stats/errors`，基于 Redis 的调用次数与错误记录；可选重置统计、清理缓存。
- **并发控制**：AI 路由中耗时同步调用（检索、Claude、Gemini、格式刷、PDF 转换等）均通过 `asyncio.to_thread` 执行，避免阻塞事件循环；Claude/Gemini 使用信号量限制并发（可选环境变量 `CLAUDE_CONCURRENT_LIMIT`、`GEMINI_CONCURRENT_LIMIT`）。

---

## 四、目录结构概览

```
copypan/
├── 【2026.03.11】项目简介.md   # 本文件
├── 开发环境清单.md             # 同事克隆、修改、上传所需环境与包清单
├── front_mic/frontend/         # 前端 SPA（Vue3 + Vite + Ant Design Vue）
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/index.js
│       ├── store/index.js      # Pinia
│       ├── assets/roundtable/  # AI 圆桌头像资源
│       └── components/         # 首页（Index.vue → Header + Search.vue）、用户、工具箱、管理、状态页等
│           ├── Search.vue      # 含传统检索与 AI 纲目面板
│           ├── toolbox/        # Cwws, BibleCo, InfoRetrieval, OutlineTranslate, ZhConvert, RoughOutline, FeastOutline, RoundTable, RoundTableDetail
│           └── management/     # ManaIndex, DataList, UploadList, UserOpts, IvOpts, AIStatsRAG, AIStatsFeast, AIStatsRoughOutline, AIStatsTranslation, AIStatsRoundtable, AIStatsSummary
└── back_mic/backend/           # 后端 FastAPI
    ├── main.py                 # 应用入口、CORS、通用 API 与路由挂载
    ├── ai_search/             # AI 纲目生成与检索、纲目翻译、简繁、格式刷与 PDF、毛胚/节期纲目
    │   ├── ai_router.py        # AI 搜索 API 路由（含 /stats/detail）
    │   ├── ai_service.py       # 核心服务逻辑
    │   ├── monitoring.py       # Redis 监控统计
    │   ├── vector_search.py    # 向量检索
    │   ├── rough_outline_prompts.py
    │   ├── feast_outline_prompts.py
    │   ├── DESIGN_SPEC_V2.md   # 双路混合检索设计规格
    │   └── 纲目制作RAG系统设计方案.md  # 纲目 RAG 技术、流程与 Prompt 汇总
    ├── roundtable/            # AI 圆桌会议（新增）
    │   ├── roundtable_router.py    # API 路由（/api/roundtable/*）
    │   ├── roundtable_service.py   # 业务逻辑与 AI 调度
    │   ├── ai_clients.py           # 统一 AI 调用封装与费用计算
    │   ├── roundtable_prompts.py   # Prompt 模板
    │   └── roundtable_db.py        # SQLite 存储（含 scene_counts 统计）
    ├── search/                 # 传统 ES 检索
    ├── user/                   # 登录、注册、改密、JWT
    ├── database/               # 上传与数据列表
    ├── format_chinese_outline.py
    ├── format_english_outline.py
    ├── es_config.py
    ├── es_init.py
    ├── scripts/                # reindex_local（7→8）、vectorize_*、add_embedding_mapping 等
    └── requirements.txt
```

---

## 五、环境与运行

- **前端**：在 `front_mic/frontend/` 下执行 `npm install`、`npm run dev`，默认开发地址如 `http://localhost:5173`；需通过环境变量（如 `VITE_API_BASE`）配置后端 API 基础地址。
- **后端**：在 `back_mic/backend/` 下安装依赖（见 `requirements.txt`），配置 `.env`（复制 `.env.example`，填写 Elasticsearch、Redis、`CLAUDE_API_KEY`、`GEMINI_API_KEY`；AI 圆桌与毛胚纲目「三分钟分享」可选 `DEEPSEEK_API_KEY`、`PERPLEXITY_API_KEY`、`OPENAI_API_KEY`、`XAI_API_KEY`），运行 `uvicorn main:app` 等。
- **Elasticsearch、Redis** 需提前部署；推荐 **Elasticsearch 8.x**。Linux 下 PDF 导出依赖 LibreOffice，Windows 下可选 Microsoft Word（docx2pdf）。

---

## 六、主要更新记录

### 2026 年 3 月

- **AI 圆桌会议**：支持六大 AI 模型（Claude、GPT、Gemini、Grok、DeepSeek、Perplexity）进行多轮协作讨论；四种场景：「十二支派」（历史神学研究）、「神学辩论」（3 轮交锋）、「**重大讨论**」（作答→互相点评→最终评价·Claude 总结）、「**顶级模型思考**」（单轮深度思考，从 Claude Opus 4.6 / GPT-5.4 / Gemini 3.1 Pro 三选一，无总结）
- **重组管理后台 AI 使用统计**：拆分为六个子页面（AI 纲目制作、节期纲目、毛胚纲目、纲目翻译、AI 圆桌、费用总览），提供更细粒度的费用与调用统计；AI 圆桌统计支持四类场景分布
- **新增 API 端点**：`/api/ai_search/stats/detail`（详细统计）、`/api/roundtable/*`（圆桌会议系列）
- **SQLite 存储**：AI 圆桌会议历史记录使用 SQLite 存储，支持场景分组统计（含 scene_three、scene_four）

---

以上为当前项目技术栈与功能的简要说明，其中 **AI 纲目面板**（首页 `Search.vue` 内）是面向讲道纲目生成的核心入口，**AI 圆桌会议**（`RoundTable.vue`）支持四种场景的多 AI 协作讨论。双路混合检索（BM25 + 向量 kNN + RRF + Jina Reranker）与方式一/方式二流程已实现，通过 `USE_VECTOR_SEARCH=true` 启用；详见 `ai_search/DESIGN_SPEC_V2.md` 与 `ai_search/纲目制作RAG系统设计方案.md`。

*最后更新：2026 年 3 月 19 日*
