# Copypan 开发环境清单

同事从 GitHub 拉取本项目并进行修改、上传时，本机需要安装的程序与包如下。

---

## 一、必装程序（系统级）

| 程序 | 版本要求 | 用途 | 安装方式示例 |
|------|----------|------|--------------|
| **Git** | 最新稳定版 | 克隆、拉取、提交、推送 | [git-scm.com](https://git-scm.com/)（Windows 选 “Git for Windows”） |
| **Python** | **3.10+**（建议 3.10 或 3.11） | 后端 FastAPI 运行 | [python.org](https://www.python.org/)（安装时勾选 “Add Python to PATH”） |
| **Node.js** | **LTS**（如 18.x / 20.x） | 前端 Vue + Vite 构建与开发 | [nodejs.org](https://nodejs.org/)（自带 npm） |
| **Elasticsearch** | **8.x**（如 8.19，与 requirements 一致） | 检索与 AI 搜索数据 | Docker 或[官网](https://www.elastic.co/downloads/elasticsearch)安装 |
| **Redis** | 最新稳定版 | 缓存、限流等 | Docker 或[官网](https://redis.io/download)安装 |

说明：

- **Elasticsearch**：项目 `requirements.txt` 要求 `elasticsearch>=8.0.0,<9.0.0`，请使用 **ES 8.x**。若本机已有 ES 7.x，可参考 `back_mic/backend/scripts/reindex_local.py` 做数据迁移。
- **Redis**：未安装或连接失败时，后端会禁用缓存但仍可运行。
- 使用 **Docker** 时，可只装 Docker Desktop，用容器跑 ES 和 Redis，无需单独装 ES/Redis 本体。

---

## 二、可选程序（按需）

| 程序 | 用途 |
|------|------|
| **Docker Desktop** | 用容器运行 Elasticsearch、Redis，便于统一环境 |
| **LibreOffice** | `docx2pdf` 转 PDF 时依赖（若不用「DOCX 转 PDF」功能可不装） |
| **VS Code / Cursor** | 推荐编辑器，便于改代码、调试 |

- **Windows**：若要用 DOCX→PDF，一般需安装 **LibreOffice** 并保证在 PATH 中；少数环境会走 Word COM，视本机而定。
- **Linux**：`sudo apt-get install libreoffice`（见 `DEPLOYMENT.md`）。

---

## 三、后端 Python 包

**路径**：`back_mic/backend/`

1. 进入后端目录并创建虚拟环境（推荐）：

   ```bash
   cd back_mic/backend
   python -m venv venv
   # Windows:
   venv\Scripts\activate
   # Linux/Mac:
   source venv/bin/activate
   ```

2. 安装依赖（清单来自 `requirements.txt`）：

   ```bash
   pip install -r requirements.txt
   ```

**当前 `requirements.txt` 中的包：**

- fastapi ≥0.100.0  
- uvicorn[standard] ≥0.22.0  
- elasticsearch ≥8.0.0,<9.0.0  
- anthropic ≥0.18.0  
- google-genai ≥1.0.0  
- openai ≥1.0.0  
- python-dotenv ≥1.0.0  
- redis ≥4.0.0  
- pydantic ≥2.0.0  
- PyJWT ≥2.0.0  
- python-multipart ≥0.0.6  
- python-docx ≥1.0.0  
- cn2an ≥0.5.17  
- zhconv ≥1.4.0  
- opencc-python-reimplemented ≥0.1.7  
- docx2pdf ≥0.1.8  

（以上由 `pip install -r requirements.txt` 一次性安装即可。）

---

## 四、前端 Node 包

**路径**：`front_mic/frontend/`

```bash
cd front_mic/frontend
npm install
```

**当前 `package.json` 中主要依赖：**

- vue ^3.4.0  
- vue-router ^4.3.0  
- pinia ^3.0.4  
- ant-design-vue ^4.2.6  
- axios ^1.13.4  
- vite ^5.2.0（开发/构建）  
- @vitejs/plugin-vue ^5.0.0  
- less ^4.5.1  

（`npm install` 会按 lock 安装全部依赖。）

---

## 五、环境配置（必做）

1. **复制环境变量示例并改名**  
   在 `back_mic/backend/` 下：
   - 复制 `.env.example` 为 `.env`
   - 不要将 `.env` 提交到 Git

2. **在 `.env` 中填写**（至少保证后端能连上 ES 和 Redis）：
   - `ES_HOST`、`ES_PORT`（若 ES 在本地且默认端口：localhost、9200）
   - ES 8.x 若开启安全：`ES_USERNAME`、`ES_PASSWORD`
   - `REDIS_HOST`、`REDIS_PORT`（本地默认：localhost、6379）
   - 需要 AI 搜索 / 纲目等功能时：  
     `CLAUDE_API_KEY`、`GEMINI_API_KEY` 等（见 `.env.example` 注释）

3. **验证**  
   - 后端：`cd back_mic/backend && uvicorn main:app --host 0.0.0.0 --port 8000`  
   - 前端：`cd front_mic/frontend && npm run dev`  
   - 健康检查：`curl http://localhost:8000/api/ai_search/health`（或浏览器访问前端）

---

## 六、Git 拉取与上传（简要）

```bash
# 克隆
git clone <仓库地址>
cd copypan

# 日常更新
git pull

# 修改后提交并推送（需有权限）
git add .
git commit -m "描述修改内容"
git push
```

---

## 七、清单小结（同事可逐项打勾）

- [ ] Git 已安装  
- [ ] Python 3.10+ 已安装，`python --version` 正常  
- [ ] Node.js（LTS）已安装，`node -v`、`npm -v` 正常  
- [ ] Elasticsearch 8.x 已安装或 Docker 已能启动 ES 8.x  
- [ ] Redis 已安装或 Docker 已能启动 Redis  
- [ ] （可选）LibreOffice 已安装（仅需 DOCX→PDF 时）  
- [ ] 后端：`back_mic/backend` 下已创建 venv 并 `pip install -r requirements.txt`  
- [ ] 前端：`front_mic/frontend` 下已执行 `npm install`  
- [ ] 已复制 `.env.example` 为 `.env` 并填写 ES、Redis 及所需 API Key  
- [ ] 能成功启动后端（uvicorn）与前端（npm run dev）  

按上述清单配置后，即可从 GitHub 拉取项目、在本地修改并上传。
