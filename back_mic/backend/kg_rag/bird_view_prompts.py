# 词典-鸟瞰纲目 Prompt
# 对应接口：POST /api/kg_rag/bird_view/skeleton 与 /bird_view/outline

BIRD_VIEW_SKELETON_PROMPT = """你是一位深入熟悉倪柝声与李常受职事的资深圣经研究者。
## 输入
纲目重点：{keyword}
相关信息：
{content}

## 任务
为这份纲目构建一个逻辑骨架——即纲目大点的推进顺序与方向。
骨架的基本推进方向是：
启示（真理根基）→ 经历（主观进入）→ 应用（实际操练）→ 目标（最终带到的地步）

## 规则
1. 只输出 JSON，绝对禁止输出代码围栏、前导文字、后置说明或任何非 JSON 字符；第一个字符必须是左花括号，最后一个字符必须是右花括号
2. 骨架为 4-7 步的有序数组，每步用一句话描述该维度的内容方向

## 输出格式
{{"skeleton": [
  {{"step": "第一步：..."}},
  {{"step": "第二步：..."}},
  {{"step": "第三步：..."}},
  {{"step": "第四步：..."}},
  {{"step": "第五步：..."}}
]}}"""


BIRD_VIEW_OUTLINE_PROMPT = """## 输入
纲目重点：{keyword}
纲目骨架：
{skeleton}
相关信息：
{content}

## 任务
我想为一篇文章写一个全面、详细并深入的鸟瞰纲目：
1、严格要求每个纲目（大纲，中纲，小纲的每个纲目）必须从原文文章中直接提取句子来构建纲目，这是必须的，不可改写，不可使用自己总结的话，不可使用自己概括的话，不可以通过概括或重述的方式改写，所有纲目的内容必须完全从原文当中直接提取。
2、必须执行：每一个大纲和每一个中纲都必须加上经节的出处，每一个大纲和每一个中纲不可太短，要有大约一行的长度，且必须直接从原文提取。
3、纲目的层级序号严格按照原文标题的层级序号，壹的下一级是一，一的下一级是1，1的下一级是a，即一级序号为壹、贰，以此类推，二级为一、二以此类推，三级为1、2以此类推，四级为a、b以此类推。
4、每一个纲目必须是一个完整的阐述，不可用短句。
5、每个纲目如果有下一级的纲目，下一级纲目至少需要2个。
6、请必须在每个纲目后边都加上圣经经节作支撑，用—连接，经节出处的格式是，创世记一章一节为：创一1，其他书卷依次类推。出来的纲目句末加经节的格式例如"如果我们恢复彼此互相的召会聚会，就会帮助圣徒成熟，预备主的新妇，将祂带回来，并引进祂的国度—来六1，启十九7。"
7、从目标文章中提取8-10个重要的经节出处，在文章在开头写出"读经：........"；如果同一书卷有多个出处，应将它们合并。例如，"启示录三章1节"和"启示录四章7节"应合并写成"启三1，四7"，同章不同节的用顿号隔开。这些提取出的经节出处应列在整个纲目的最前面。
8、纲目的序号与纲目中间必须用Tab键连接，不可用顿号，全角空格或其他的符号链接。
9、若一条纲目有下一级纲目，则这一条纲目句尾需加冒号，若这一条纲目没有下一级纲目，则这一条纲目句尾需加句号，题目后面不需要加经节。
10、纲目中不需要双引号，所有的双引号请去掉。请记住纲目中的所有单引号必须用中文状态下的单引号，不可用英文状态下的单引号。
11、一条纲目句中若有句号，需将句号改成分号。
12、两个数字之间需要用全角的～链接。
13、输出的纲目中不可以有重复的内容，不可以出现两条一样的纲目。
14、请不要写python代码来生成纲目，不要生成txt或docx，而是直接生成纲目。
15、将鸟瞰的纲目写在代码块中。

## 规则
1、这个一篇内容的主题是：{keyword}，请围绕主题{keyword}建立鸟瞰纲目结构，纲目需要涵盖文章中所有与{keyword}最直接相关的点，每个大点需要与{keyword}直接相关，意思就是大点这部分必须包含{keyword}这个词。
2、纲要必须执行：每个纲目（大纲，中纲，小纲的每个纲目）必须从原文文章中直接提取句子来构建纲目，不可改写，不可使用自己总结的话。
3、不是简单的按顺序列出，要综合文章的所有点，给出关于主题创造的鸟瞰纲目，内容全面，结构清晰，逻辑合理。
4、输出的纲目中不可以有重复的内容，不可以出现两条一样的纲目。
5、纲目大点的排列顺序以骨架步骤为准，不可自行调整；骨架已按启示→经历→应用→目标的脉络构建，每个大点的展开须忠于该步骤的方向。
6、生成的纲目内容，控制在A4纸两页以内。
7、务必执行：不许有任何自己生成总结的话来做大点！！！！！"""
