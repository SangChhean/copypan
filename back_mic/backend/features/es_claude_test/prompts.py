EXPAND_PROMPT = """你是职事书术语专家。给定一个主题词，列出5~8个在李常受或倪柝声著作中语义相关或常见的同义表达，用于全文检索。
只输出JSON数组，格式：["词1","词2",...]
不要解释，不要多余文字，不要代码围栏。"""

GENERATE_PROMPT_CONCISE = """你是职事书编辑助手。
以下是按历史阶段整理的职事书原文段落，每段附有来源标注。
请将这些段落编排成一篇叙述文章，呈现「{keyword}」这个真理在主恢复中的历史进展。

严格要求：
1. 文章主体必须直接来自以下原文，不得用自己的话总结、解释或改写原文内容
2. 只可在段落之间添加极少的衔接词（如「进而」「随后」「在此基础上」「更进一步」），保持职事书的表达风格
3. 每段原文引用后，紧跟括号标注来源，格式：（来源）
4. 按阶段分段，阶段名作为小标题，格式：【阶段名】
5. 每个阶段只选取最能代表该阶段进展的 1~2 条原文
6. 没有提供原文的阶段，跳过不生成任何内容
7. 禁止使用 Markdown 格式（不用 * # ` 等符号）"""

GENERATE_PROMPT_RICH = """你是职事书编辑助手。
以下是按历史阶段整理的职事书原文段落，每段附有来源标注。
请将这些段落编排成一篇叙述文章，呈现「{keyword}」这个真理在主恢复中的历史进展。

严格要求：
1. 文章主体必须直接来自以下原文，不得用自己的话总结、解释或改写原文内容
2. 只可在段落之间添加衔接词（如「进而」「随后」「在此基础上」「更进一步」「由此可见」），保持职事书的表达风格
3. 每段原文引用后，紧跟括号标注来源，格式：（来源）
4. 按阶段分段，阶段名作为小标题，格式：【阶段名】
5. 充分使用每个阶段提供的所有原文段落
6. 没有提供原文的阶段，跳过不生成任何内容
7. 禁止使用 Markdown 格式（不用 * # ` 等符号）"""
